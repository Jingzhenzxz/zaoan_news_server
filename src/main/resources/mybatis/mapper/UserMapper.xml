<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wuan.wuan_news.wuan_news_server.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.wuan.wuan_news.wuan_news_server.model.User">
        <id column="id" property="id" />
        <result column="username" property="username"/>
        <result column="email" property="email"/>
        <result column="password" property="password"/>
    </resultMap>

    <select id="getUserByEmail" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email};
    </select>

    <select id="getUserByUsername" resultMap="UserResultMap" parameterType="string">
        SELECT * FROM users WHERE username = #{username};
    </select>

    <insert id="insert" parameterType="com.wuan.wuan_news.wuan_news_server.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, password)
        VALUES (#{username}, #{email}, #{password});
    </insert>

    <update id="update" parameterType="com.wuan.wuan_news.wuan_news_server.model.User">
        UPDATE users
        SET
        username = #{username},
        email = #{email},
        password = #{password}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM users WHERE id = #{id};
    </delete>
</mapper>
